#ifndef _SERIAL_COMMANDS_H_
#define _SERIAL_COMMANDS_H_

#include <driver/gpio.h>

// ============================================================
// SBUS 硬件 UART 配置 (宇树 Go2 Air)
// 协议: 100000bps, 8E2 (8数据+偶校验+2停止), 信号反相
// ============================================================
#define SBUS_UART_PORT      UART_NUM_1
#define SBUS_TX_PIN         GPIO_NUM_47   // TX → 接 Go2 SBUS RX
#define SBUS_RX_PIN         GPIO_NUM_21   // RX (本场景不用, 预留)
#define SBUS_BAUD_RATE      100000

// ============================================================
// SBUS 通道常量 (不要改)
// ============================================================
#define SBUS_NEUTRAL        992     // 摇杆中立 (所有通道)
#define SBUS_LOW            192     // Ly 前进后退 最小值
#define SBUS_HIGH           1792    // Ly 前进后退 最大值
#define SBUS_LX_LEFT        432     // Lx/Rx 左 最大值
#define SBUS_LX_RIGHT       1552    // Lx/Rx 右 最大值
#define SBUS_CH9_FIXED      1690    // ch[8] 固定值
#define SBUS_CH10_FIXED     154     // ch[9] 固定值

// ============================================================
// 移动参数 —— 改这里
// ============================================================
#define MOVE_DURATION_MS    1500    // 移动持续毫秒
#define MOVE_SPEED_PCT      60      // 速度百分比 (10~100), 越大越快
//                                  100 = 全速 (1792), 60 = 六成速
//                                  摇杆值渐变: 每14ms步进80

// ============================================================
// 命令列表 —— AI 可识别的所有命令
// 格式: X("命令名")
// 添加命令只需加一行, 具体 SBUS 参数在 .cc 文件里改
// ============================================================
#define SERIAL_COMMANDS \
    X("停下来")   \
    X("站立模式") \
    X("卧倒")     \
    X("蹲下")     \
    X("跑步模式") \
    X("前进")     \
    X("后退")     \
    X("向左走")   \
    X("向右走")   \
    X("向左转弯") \
    X("向右转弯")

#endif // _SERIAL_COMMANDS_H_
